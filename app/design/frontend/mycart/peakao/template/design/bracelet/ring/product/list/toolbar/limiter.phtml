<?php /* @var $toolbar Mage_Catalog_Block_Product_List_Toolbar */?>
<?php $toolbar = $this->getParentBlock();?>

<label><?php echo $toolbar->__('Show') ?></label>
			<select id="rings-limiter">
			<?php foreach ($toolbar->getAvailableLimit() as  $_key=>$_limit): ?>
				<option value="<?php echo $_limit ?>"<?php if($toolbar->isLimitCurrent($_key)): ?> selected="selected"<?php endif ?>>
					<?php echo $_limit ?>
				</option>
			<?php endforeach; ?>
			</select><?php echo $toolbar->__('per page') ?>
<script>

jQuery(function(){

	var getData = function(){
		var value = jQuery("#rings-limiter").val();
		return value;
	};
	ringsFilter.registerMetadata("limit", getData);

	jQuery("#rings-limiter").change(function(){
		ringsFilter.updateResults();
	});
});

</script>
